# Expression Wizard Backlog

这份文档记录尚未完成、值得以后逐步验证或实现的想法。

- 已经完成的实验结论写入 [`EXPRESSION_EXPERIMENT_REPORT.md`](EXPRESSION_EXPERIMENT_REPORT.md)。
- Backlog 条目保留提出日期、动机、下一步和完成标准，避免想法只留在聊天记录里。
- 优先级会随着实验结果调整；进入实施时，再拆分为具体任务。

## 状态与优先级

- 状态：`想法`、`待研究`、`待实验`、`待设计`、`进行中`、`已完成`、`暂缓`
- 优先级：P0（当前关键阻碍）、P1（近期重要）、P2（后续改进）、P3（探索性）

## 当前 Backlog

### EW-001：牙齿感知的嘴部参数对照实验

| 字段 | 内容 |
|---|---|
| 状态 | 待实验 |
| 优先级 | P0 |
| 提出日期 | 2026-08-16 |
| 参数 | `aaa`、`smile` |

观察与动机：

- 当前闭嘴 Anchor 1 上，`aaa` 倾向于生成并露出下齿，`smile` 倾向于生成并露出上齿。
- 算法补出的牙齿未必符合角色设定；Lys 是吸血鬼，应特别关注犬齿是否保留、消失或被普通牙列替代。
- 当前露齿结果只能作为嘴形参考，不能直接认定为可靠的 Lys LoRA 训练图。

下一步：

1. 准备构图、身份和姿态尽量一致的三种新输入：嘴微张但不明显露齿、自然露齿、明确露出 Lys 犬齿。
2. 保留闭嘴 Anchor 1 作为对照组。
3. 对四种输入分别重复 `aaa` 与 `smile` 的正向单参数扫描。
4. 固定其他参数、裁切设置、候选数量与输出尺寸。
5. 逐张标记上齿、下齿、犬齿的保留、重绘、消失、错位和身份漂移。

完成标准：

- 明确输入嘴部状态对 `aaa`、`smile` 牙齿输出的影响。
- 为 Lys 找到至少一套能稳定保留犬齿的输入与参数范围；如果无法做到，则记录限制并规划后处理方案。

### EW-002：嘴部参数推荐候选与安全范围

| 字段 | 内容 |
|---|---|
| 状态 | 待实验 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

人工复核 `aaa`、`eee`、`woo`、`smile` 的现有候选，标记自然工作区、夸张区、牙齿风险区和推荐帧。EW-001 完成后再最终确定露齿范围。

### EW-003：剩余单参数扫描

| 字段 | 内容 |
|---|---|
| 状态 | 待实验 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

- 确认 `wink` 的自然上限和潜在嘴部耦合。
- 扫描 `eyebrow` 负方向。
- 扫描 `pupil_x`、`pupil_y`。
- 扫描 Pitch、Yaw、Roll。

### EW-004：组合表情配方

| 字段 | 内容 |
|---|---|
| 状态 | 待设计 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

在单参数安全范围确认后，建立开心、悲伤、愤怒、惊讶等组合表情；保存参数配方、来源 Anchor、人工评分和已知副作用。

### EW-005：角色特征保护与训练图质量门槛

| 字段 | 内容 |
|---|---|
| 状态 | 想法 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

为犬齿、瞳色、发型、面部标记等角色关键特征建立检查项。候选图只有通过角色身份、局部结构、牙齿设计和伪影检查后，才能升级为 LoRA 训练素材。

### EW-006：模型资料驱动的生成配置

| 字段 | 内容 |
|---|---|
| 状态 | 进行中 |
| 优先级 | P1 |
| 提出日期 | 2026-08-15 |

每个新 checkpoint 先核对模型作者或介绍页，建立与具体文件哈希绑定的 Model Profile，包括原生分辨率、sampler、scheduler、steps、CFG、Clip Skip、VAE 和提示词模板。实验阶段默认跳过 upscale。

### EW-007：Expression Wizard UI 与实验复核体验

| 字段 | 内容 |
|---|---|
| 状态 | 待设计 |
| 优先级 | P2 |
| 提出日期 | 2026-08-15 |

继续改善 Grid/Focus、候选比较、人工标记、推荐范围和实验报告入口，让参数实验与结论记录能在 EW 内形成闭环。

### EW-008：眉毛分区关键点控制

| 字段 | 内容 |
|---|---|
| 状态 | 待研究 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

观察与动机：

- 当前 `eyebrow` 参数的幅度和局部自由度有限。
- 眉毛是表情可读性的重要来源；眉头、眉弓、眉尾往往需要不同方向和不同强度的变化。

候选方向：

1. 检查现有 AdvancedLivePortrait 表情编码是否包含可拆分利用的眉部通道。
2. 评估面部 landmark、控制点或局部形变方法，分别控制眉头、眉弓和眉尾。
3. 设计左右眉独立与联动两种模式，同时限制眼睛、额头和身份漂移。
4. 在 EW 中用可视化控制点或分区滑杆呈现，而不是只提供一个总 `eyebrow` 参数。

完成标准：

- 至少能独立控制眉头、眉弓、眉尾中的两个区域，并证明其余主要面部区域保持稳定。
- 为悲伤、愤怒、惊讶和疑惑各保存一套可复现的眉毛配方。

### EW-009：肌肉运动与皮肤褶皱增强阶段

| 字段 | 内容 |
|---|---|
| 状态 | 待研究 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

观察与动机：

- ExpressionEditor 能改变嘴、眼和部分眉部几何，但不能可靠生成眼皮褶皱、鼻部褶皱和完整的肌肉挤压。
- 少量嘴角阴影不等同于可信的皮肤折叠。
- 对 LoRA 表情训练而言，只有几何变化、缺少解剖与皮肤细节的图像可能显得僵硬。

研究原则：

1. 把 EW/ExpressionEditor 保留为可控的第一阶段几何与构图工具。
2. 调研第二阶段局部增强方案，例如受遮罩约束的重绘、表情驱动扩散、局部 ControlNet/landmark 条件、专用面部编辑模型或人工修图。
3. 优先局部处理眼皮、眉间、鼻根/鼻翼、法令区和嘴角，避免整张脸重绘造成身份漂移。
4. 每种方案比较结构可信度、Lys 身份保持、犬齿等角色特征保持、可重复性和运行成本。
5. 在确定方案前不把任何特定工具写成既定依赖。

完成标准：

- 找到至少一种可重复的第二阶段方法，能在保持身份的同时增加一种目标细节（例如闭眼眼皮褶皱或皱鼻纹）。
- 建立“几何正确”和“肌肉/皮肤可信”两个独立的人工评分维度。
- 明确哪些候选可以直接进入 LoRA 数据集，哪些必须经过局部增强。

### EW-010：静默启动与后台生命周期管理

| 字段 | 内容 |
|---|---|
| 状态 | 已完成 |
| 优先级 | P1 |
| 提出日期 | 2026-08-16 |

目标：

- 台式机 Gateway 和笔记本 EW 后台启动后不持续显示 PowerShell/命令行窗口。
- 用户仍能看见运行状态、找到日志并可靠停止服务，避免隐藏的孤儿进程。
- 浏览器标签或整个浏览器关闭时，行为明确且不破坏正在执行的任务。

建议生命周期：

1. 启动器先检查健康接口和端口；已运行时只打开网页，未运行时才创建单一后台实例。
2. 后台进程使用隐藏窗口启动，标准输出和错误写入轮转日志；不得因隐藏窗口而丢失诊断信息。
3. 关闭浏览器标签默认不立即停止后台。浏览器可能崩溃或误关，且关闭事件不可靠；正在生成的任务必须继续安全运行。
4. UI 提供明确的“停止 Expression Wizard”操作；Windows 同时提供独立的 Start/Status/Stop 入口。
5. 停止时先拒绝新任务，再等待当前任务完成或由用户确认取消，随后执行优雅关机并清理 PID/状态文件。
6. 可选自动停止：没有连接的页面、没有运行或排队任务，并持续空闲达到用户设定时间后，笔记本 EW 后台自动退出。
7. 台式机 Gateway 默认按登录会话长期运行，直到用户明确停止、注销或关机；它不应跟随笔记本浏览器标签关闭。
8. 使用 PID 文件、进程启动时间、健康接口和端口共同验证进程身份，避免停止无关的 Python 程序。
9. 停止接口仅允许本机访问并要求一次性/本机管理凭据；LAN 客户端不能直接关闭台式机 Gateway。

建议默认值：

- 关闭标签：后台继续运行。
- 正在运行或排队的任务：禁止自动退出。
- 笔记本后台空闲自动退出：默认关闭，用户可选择 30 分钟、2 小时或永不退出。
- 台式机 Gateway：登录后静默常驻，手动停止或随系统会话结束。

完成标准：

- 双击一个无常驻控制台窗口的入口即可启动或重新打开 EW。
- 重复启动不会产生多个后台实例。
- 浏览器关闭、崩溃和重新打开后，运行中任务与历史状态仍可恢复。
- 用户可从 UI 或 Windows 独立入口查看状态、打开日志并优雅停止。
- 台式机和笔记本重启后不存在失效 PID、误判端口或无法停止的残留进程。

2026-08-16 实现进展：静默 Start/Status/Stop、单实例校验、轮转日志、浏览器心跳、
可选空闲退出、本机认证关闭接口和活动任务保护已经完成。台式机启动器还会在 ComfyUI
离线时启动 Comfy Desktop，等待 8188 健康后再启动 Gateway；它不会重复启动、强杀或
随 Gateway 一起关闭 ComfyUI。当前用户登录后自动启动也已实现；托盘图标仍可作为后续增强。

部署验证：台式机 `Expression Wizard Managed Gateway` 任务已启用，登录触发延迟为 15 秒；
旧直连任务已备份并禁用。台式机 8189 与笔记本 8775 均由新版管理器启动并通过健康检查，
笔记本 EW 报告远程 ComfyUI `online: true`。

## 已完成条目

条目完成后移到这里，并附实现提交或实验任务 ID；不要删除历史条目。

- `EW-010`：静默启动、浏览器独立生命周期、ComfyUI 前置启动和台式机登录自动启动。
  主要实现提交：`c2deba2`、`c861725`、`d708d2b`、`e6c153a`。
