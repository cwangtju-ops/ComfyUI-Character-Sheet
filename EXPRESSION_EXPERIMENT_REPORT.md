# Expression Wizard 实验报告

这份文档持续记录 Lys 在 AdvancedLivePortrait `ExpressionEditor` 中的参数实验。
每次实验都应保存任务 ID、输入版本、参数范围、数值分析、人工观察与下一步，
避免结论只存在于聊天记录中。

## 实验规则

- 固定角色与 Anchor，单次实验只改变一个参数。
- 保留首个零参数候选作为视觉基线（双向参数实验可引用已有基线）。
- 同时记录数值隔离结果和人工视觉判断；两者描述的是不同层面。
- 人工视觉判断优先用于身份保持、表情自然度和局部串扰结论。
- 实验阶段不使用 upscale。
- 每个任务保存 PNG、原始 `.exp`、安全 JSON/CSV、workflow 和 manifest。

## 环境基线

| 项目 | 值 |
|---|---|
| 日期 | 2026-08-16 |
| EW 分支 | `feature/distributed-ui-dev-mode` |
| 分布式表达式导出提交 | `13eb01a` |
| 输入 | `anchor 1.png` |
| 输入尺寸 | 1254 × 1254 |
| 输入 SHA-256 | `f9053aa67feb3652046897db8ccb9215f8c26987f178cf9c8cfb9d79fee59f4d` |
| 固定参数 | 除被扫描参数外均使用 ExpressionEditor 默认值 |
| 强制设置 | `sample_ratio=0`, `sample_parts=OnlyExpression` |
| `src_ratio` | 1.0 |
| `crop_factor` | 1.7 |
| 数据根目录 | `C:\Codex Projects\ComfyUI\Character Sheet_Lys\ComfyUI_Generated\Expression_Wizard` |

### 基线人工观察

- 零参数输出相对原 Anchor 1 有轻微的整头向上平移。
- 人脸身份保持稳定，没有可见的 face drift。
- 该轻微全局对齐变化与表情参数造成的局部变化应分开评价。

## 2026-08-16 单参数实验

### Smile：嘴部微笑

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_014308_sweep_smile` |
| 范围 | 0.00 → 0.55，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 31, 71, 131, 141, 161, 171, 172, 201 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_014308_sweep_smile` |

人工结论：

- 所有候选只观察到嘴部变化。
- 眼睛、眉毛、鼻部、脸型与身份保持稳定。
- 0.00 → 0.55 的变化平滑，强度逐步增加。
- 这一范围适合轻微到中等嘴部微笑；完整“开心”表情以后需要与眼睛、眉毛组合。

### Blink：双眼闭合

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015652_sweep_blink` |
| 范围 | 0.00 → -10.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 11, 21, 111, 131, 151, 161 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015652_sweep_blink` |

当前观察：

- 变化集中在双眼，其他主要面部区域保持稳定。
- 约 -4.55 表现为自然的半眯眼。
- -10 已接近闭眼，但仍保留一条眼缝。
- 常用测试范围暂定为 0 到 -10；暂时不需要触及允许下限 -20。

### Eyebrow：正方向眉形变化

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015813_sweep_eyebrow` |
| 范围 | 0.00 → 10.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 11, 21 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015813_sweep_eyebrow` |

当前观察：

- 数值变化只涉及两个眉部编码，隔离性很好。
- 视觉变化比 blink 更细微，集中在眉形和眉位。
- 尚未测试负方向；正负方向可能表达不同情绪，不能假设完全对称。

### Wink：单眼闭合

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015927_sweep_wink` |
| 范围 | 0.00 → 15.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 31, 111, 131, 170, 171 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015927_sweep_wink` |

当前观察：

- 主要闭合画面左侧眼睛。
- 约 6.82 已形成明显单侧眯眼；15 为强烈单眼闭合。
- 高强度时眉部与嘴缝可能出现轻微视觉耦合，需人工逐张确认后再确定推荐范围。
- 因为涉及 5 个编码，不能仅凭“固定参数违规为 0”断言它只改变眼睑。

## 本轮基础设施发现与修复

1. Comfy Desktop 优先读取 Shared 模型目录，而不是独立 ComfyUI 根目录。
2. Shared `warping_module.safetensors` 是 25,853,952 字节的截断下载。
3. 损坏文件已保留为：
   `warping_module.safetensors.corrupt-backup-20260816`。
4. 已从验证通过的模型目录补齐：
   `warping_module`、`spade_generator`、`stitching_retargeting_module`。
5. `.exp` 含 Torch Tensor，现由台式机 Gateway 在 ComfyUI Python 环境中转换为安全 JSON；
   笔记本无需安装 PyTorch。实现提交：`13eb01a`。

## 下一步实验队列

1. 人工确认 Wink 的自然可用上限与潜在嘴部耦合。
2. Eyebrow 负方向单参数扫描。
3. `aaa`、`eee`、`woo` 嘴形扫描。
4. `pupil_x`、`pupil_y` 视线扫描。
5. Pitch、Yaw、Roll 姿态扫描。
6. 在单参数安全范围确定后，构建开心、悲伤、愤怒、惊讶等组合表情。

## 记录约定

后续新增实验应追加到本文档，不覆盖旧结论。若人工复核改变了判断，保留原结论并注明
修订日期、原因和新结论。
