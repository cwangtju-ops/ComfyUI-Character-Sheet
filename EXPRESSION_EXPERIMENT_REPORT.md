# Expression Wizard 实验报告

这份文档持续记录 Lys 在 AdvancedLivePortrait `ExpressionEditor` 中的参数实验。
每次实验都应保存任务 ID、输入版本、参数范围、数值分析、人工观察与下一步，
避免结论只存在于聊天记录中。

## 实验规则

- 固定角色与 Anchor，单次实验只改变一个参数。
- 保留首个零参数候选作为视觉基线（双向参数实验可引用已有基线）。
- 同时记录数值隔离结果和人工视觉判断；两者描述的是不同层面。
- 人工视觉判断优先用于身份保持、表情自然度和局部串扰结论。
- 实验阶段不使用 upscale。
- 每个任务保存 PNG、原始 `.exp`、安全 JSON/CSV、workflow 和 manifest。

## 环境基线

| 项目 | 值 |
|---|---|
| 日期 | 2026-08-16 |
| EW 分支 | `feature/distributed-ui-dev-mode` |
| 分布式表达式导出提交 | `13eb01a` |
| 输入 | `anchor 1.png` |
| 输入尺寸 | 1254 × 1254 |
| 输入 SHA-256 | `f9053aa67feb3652046897db8ccb9215f8c26987f178cf9c8cfb9d79fee59f4d` |
| 固定参数 | 除被扫描参数外均使用 ExpressionEditor 默认值 |
| 强制设置 | `sample_ratio=0`, `sample_parts=OnlyExpression` |
| `src_ratio` | 1.0 |
| `crop_factor` | 1.7 |
| 数据根目录 | `C:\Codex Projects\ComfyUI\Character Sheet_Lys\ComfyUI_Generated\Expression_Wizard` |

### 基线人工观察

- 零参数输出相对原 Anchor 1 有轻微的整头向上平移。
- 人脸身份保持稳定，没有可见的 face drift。
- 该轻微全局对齐变化与表情参数造成的局部变化应分开评价。

## 2026-08-16 单参数实验

### Smile：嘴部微笑

| 方向 | 任务 ID | 范围 | 候选数 |
|---|---|---:|---:|
| 正向精细 | `20260816_014308_sweep_smile` | 0.00 → 0.55 | 12 |
| 正向全范围 | `20260816_021118_sweep_smile` | 0.00 → 1.30 | 12 |
| 负向全范围 | `20260816_021230_sweep_smile` | 0.00 → -0.30 | 12 |

三组实验的张量强度 R² 均约为 1.0；非零编码均为
31、71、131、141、161、171、172、201；固定参数违规均为 0。

人工结论：

- 0.00 → 0.55 的所有候选只观察到嘴部变化。
- 眼睛、眉毛、鼻部、脸型与身份保持稳定。
- 0.00 → 0.55 的变化平滑，强度逐步增加。
- 约 0.95 后进入明显露齿笑，1.30 为强笑；高强度时眼睑视觉上可能略微收紧，
  需人工判断它是自然“笑眼”还是不希望的耦合。
- 负方向至 -0.30 都较温和，主要产生轻微嘴角下压和更严肃的感觉。
- 初步工作范围：轻微/中等微笑 0.00–0.55，明显笑容约 0.55–0.95；
  0.95–1.30 作为强表情区保留。
- 完整“开心”表情以后仍需要与眼睛、眉毛组合。

### AAA：纵向张嘴/下颌

| 方向 | 任务 ID | 范围 | 候选数 |
|---|---|---:|---:|
| 正向 | `20260816_020423_sweep_aaa` | 0.00 → 120.00 | 12 |
| 负向 | `20260816_020531_sweep_aaa` | 0.00 → -30.00 | 12 |

两组实验的张量强度 R² 均约为 1.0；非零编码均为 171、191、192；
固定参数违规均为 0。

当前观察：

- 正方向逐步增加纵向张嘴和下颌开度，变化集中在嘴部。
- 约 33 已形成清晰张嘴，约 65 是强发音，90–120 更适合夸张发音或惊讶素材。
- 120 仍能保持身份与整体结构，但属于极端表达，不建议作为普通对话默认值。
- 负方向逐步压紧、压薄嘴唇；约 -13.6 仍较自然，-30 已是明显压抿极值。
- 初步工作范围：普通发音约 0–65；强张嘴 65–90；90–120 作为极端区。

### EEE：横向嘴形

| 方向 | 任务 ID | 范围 | 候选数 |
|---|---|---:|---:|
| 正向 | `20260816_020650_sweep_eee` | 0.00 → 15.00 | 12 |
| 负向 | `20260816_020746_sweep_eee` | 0.00 → -20.00 | 12 |

两组实验的张量强度 R² 均约为 1.0；非零编码均为 141、201、202；
固定参数违规均为 0。

当前观察：

- 正方向横向展开嘴角并逐渐露齿；约 6.8 已形成明显 “ee” 嘴形。
- 15 接近夸张咧嘴，牙列和唇形的视觉风险开始上升。
- 负方向横向收窄嘴唇并逐步噘起；约 -9.1 尚可用于轻微收嘴，-20 为极端小嘴形。
- 正负方向表达不同动作，不能把负值理解为简单“减少 EEE”。
- 初步工作范围：正向 0–7，负向 0–-10；其余作为夸张区。

### WOO：圆唇/中央嘴缝

| 方向 | 任务 ID | 范围 | 候选数 |
|---|---|---:|---:|
| 正向 | `20260816_020858_sweep_woo` | 0.00 → 15.00 | 12 |
| 负向 | `20260816_021006_sweep_woo` | 0.00 → -20.00 | 12 |

两组实验的张量强度 R² 均约为 1.0；非零编码均为 31、71、141、172；
固定参数违规均为 0。

当前观察：

- 正方向让嘴唇变圆并形成轻微中央开口；0–15 整体较温和、稳定。
- 负方向并非简单取消圆唇，而是逐渐横向拉开嘴缝；-20 已接近轻微露齿/咧嘴。
- WOO 负方向与 EEE 正方向有部分视觉相似，但底层编码集合不同，组合时不应互换。
- 初步工作范围：正向 0–15；负向优先限制在 0–-10，-10 以下作为实验区。

### 嘴部参数横向结论

- `aaa` 主要控制纵向开口和下颌。
- `eee` 主要控制横向展开/收窄。
- `woo` 主要控制圆唇，但负方向会出现横向嘴缝变化。
- `smile` 控制笑容轨迹，影响编码最多；高强度最可能带来自然或非自然的复合变化。
- 所有方向的张量幅度都高度线性，但视觉自然度并不随数值线性变化。

### 牙齿呈现与输入图像依赖

2026-08-16 人工观察：

- 在当前闭嘴的 `anchor 1.png` 上，`aaa` 增大时倾向于生成并露出下齿。
- `smile` 增大时倾向于生成并露出上齿。
- 因此，`aaa` 和 `smile` 不只是改变嘴唇与下颌几何；当输入图像没有提供可见牙齿时，
  AdvancedLivePortrait 还可能自行补全牙齿外观。
- 补全出的牙齿未必符合角色设计。Lys 是吸血鬼，普通整齐牙列可能遗漏犬齿，
  或生成与设定不一致的牙齿形状。
- 本轮结果可以用于研究嘴形运动，但不能据此认定牙齿身份与细节已经得到保持。

后续对照实验：

1. 保留当前闭嘴 Anchor 1 作为对照组。
2. 准备同一角色、尽量保持姿态和构图一致的“嘴微张但不明显露齿”原图。
3. 准备“自然露出牙齿”的原图。
4. 准备明确显示 Lys 犬齿设计的原图。
5. 对每张输入重复 `aaa` 与 `smile` 的单参数正向扫描；其他参数、裁切设置和候选数保持一致。
6. 比较下齿、上齿、犬齿的保留程度，以及嘴形变化过程中是否发生牙齿重绘、消失、错位或身份漂移。

在上述对照完成前，涉及露齿的候选应标记为“嘴形参考”，不直接视为 Lys 最终训练图。

### 面部结构、肌肉与皮肤细节的能力边界

2026-08-16 人工观察：

- 当前 `ExpressionEditor` 主要表现为已有面部区域的几何位移和开合，不能可靠地创造新的面部结构细节。
- 嘴部运动会带来少量嘴角阴影，但这种变化不足以代表完整的肌肉挤压或皮肤折叠。
- 闭眼时没有可靠生成相应的眼皮褶皱；鼻部运动也不能生成自然的鼻梁、鼻翼或鼻根褶皱。
- 眉毛调整幅度和局部自由度有限，而眉头、眉弓、眉尾的独立变化对生动表情非常重要。
- 因此，参数数值隔离成功不等于表情的解剖结构和皮肤细节完整。

当前解释与限制：

- 以上是基于 Lys Anchor 1 和本轮候选图的人工结论，尚不能断言所有输入图像和所有版本都完全相同。
- 后续评估应把“几何姿态正确”和“肌肉/皮肤表现可信”分成两个评分维度。
- 当前 EW 输出适合用作表情构图和运动参考；需要明显眼皮、鼻部或皮肤褶皱的最终训练图，暂不应只依赖 ExpressionEditor。

### Blink：双眼闭合

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015652_sweep_blink` |
| 范围 | 0.00 → -10.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 11, 21, 111, 131, 151, 161 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015652_sweep_blink` |

当前观察：

- 变化集中在双眼，其他主要面部区域保持稳定。
- 约 -4.55 表现为自然的半眯眼。
- -10 已接近闭眼，但仍保留一条眼缝。
- 常用测试范围暂定为 0 到 -10；暂时不需要触及允许下限 -20。

### Eyebrow：正方向眉形变化

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015813_sweep_eyebrow` |
| 范围 | 0.00 → 10.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 11, 21 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015813_sweep_eyebrow` |

当前观察：

- 数值变化只涉及两个眉部编码，隔离性很好。
- 视觉变化比 blink 更细微，集中在眉形和眉位。
- 尚未测试负方向；正负方向可能表达不同情绪，不能假设完全对称。

### Wink：单眼闭合

| 项目 | 结果 |
|---|---|
| 任务 ID | `20260816_015927_sweep_wink` |
| 范围 | 0.00 → 15.00，12 个等距候选 |
| 张量强度 R² | 约 1.0 |
| 非零编码 | 31, 111, 131, 170, 171 |
| 固定参数违规 | 0 |
| 文件夹 | `20260816_015927_sweep_wink` |

当前观察：

- 主要闭合画面左侧眼睛。
- 约 6.82 已形成明显单侧眯眼；15 为强烈单眼闭合。
- 高强度时眉部与嘴缝可能出现轻微视觉耦合，需人工逐张确认后再确定推荐范围。
- 因为涉及 5 个编码，不能仅凭“固定参数违规为 0”断言它只改变眼睑。

## 本轮基础设施发现与修复

1. Comfy Desktop 优先读取 Shared 模型目录，而不是独立 ComfyUI 根目录。
2. Shared `warping_module.safetensors` 是 25,853,952 字节的截断下载。
3. 损坏文件已保留为：
   `warping_module.safetensors.corrupt-backup-20260816`。
4. 已从验证通过的模型目录补齐：
   `warping_module`、`spade_generator`、`stitching_retargeting_module`。
5. `.exp` 含 Torch Tensor，现由台式机 Gateway 在 ComfyUI Python 环境中转换为安全 JSON；
   笔记本无需安装 PyTorch。实现提交：`13eb01a`。

## 下一步实验队列

1. 准备嘴微张、自然露齿和 Lys 露犬齿的对照输入，重复 `aaa`、`smile` 单参数扫描。
2. 研究眉头、眉弓、眉尾可否独立控制，并建立眉毛控制原型。
3. 评估能补充肌肉挤压、眼皮和鼻部褶皱的第二阶段工具或工作流。
4. 人工确认 Wink 的自然可用上限与潜在嘴部耦合。
5. Eyebrow 负方向单参数扫描。
6. 人工复核本轮嘴部参数的初步工作范围，并标记推荐候选。
7. `pupil_x`、`pupil_y` 视线扫描。
8. Pitch、Yaw、Roll 姿态扫描。
9. 在单参数安全范围确定后，构建开心、悲伤、愤怒、惊讶等组合表情。

## 记录约定

后续新增实验应追加到本文档，不覆盖旧结论。若人工复核改变了判断，保留原结论并注明
修订日期、原因和新结论。
